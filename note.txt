Cose interessanti:
1) Istogramma in funzione di T (a T basse la distribuzione si aggira attorno a -N, per T alte si sposta vicino a 0. Questo siccome la grandezza minimizzata e` energia libera di Helmholtz, non energia interna)
2) Istogramma in funzione di N (con N basso si creano dei gap nell'istogramma. Alcune energie non sono raggiungibili)
3) Un modo interessante per stimare il burn-in in una mcmc e misurare la magnetizzazione media al variare del burn-in stesso, e vedere dove perde la dipendenza da quel parametro
4) Per N grande (>25 per dim=2) il grafico della magnetizzazione per T-->0 ha un tratto molto basso. Possibile errore numerico. Nuova idea: potrebbe essere dovuto ad una sottostima del burn_in. Da testare in modo accurato. TODO: studio della lunghezza del burn_in in funzione di N, dim, T.

TODO:
Funzione di autocorrelazione Gamma. (per thinning)
Alternativa: binning 

Init model with same spins.

plottare diversi N sullo stesso plot (insieme a curva analitica, e vedere convergenza)

fare studio di tempo burn_in

plot magnetizzazione senza modulo (hist)

studio con campo magnetico

modificare metropolis a cambiare piu spin insieme

